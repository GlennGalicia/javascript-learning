const producto = 'Monitor 4k'
const texto = 'en promoción !!'.repeat(3)
const hobbies = 'leer, escribir, python, css, html, sass, gulp'.split(', ')

console.log(producto);
console.log(`${producto} ${texto}`);
console.log(hobbies);
