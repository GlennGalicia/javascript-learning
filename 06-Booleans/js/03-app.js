const autenticado = true

if (autenticado)
    console.log('Si estas autenticado');
else
    console.log('No estas autenticado');

// Operador ternario
console.log(autenticado ? 'Si estas autenticado maestro' : 'No estas autenticado');
